<Window x:Class="TimeAnchor.MainWindow"
        Icon="TimeAnchor.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="TimeAnchor" Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" Grid.Row="0" materialDesign:ElevationAssist.Elevation="Dp4">
            <Grid>
                <TextBlock Text="TimeAnchor" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22" FontWeight="Bold"/>

                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button x:Name="BtnCalendar" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Black" Height="Auto" Padding="10,5" Click="BtnCalendar_Click" ToolTip="Takvimi Göster">
                        <StackPanel>
                            <TextBlock x:Name="TxtLiveTime" Text="00:00:00" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtLiveDate" Text="00.00.0000" FontSize="12" HorizontalAlignment="Center" Opacity="0.85" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Button>

                    <Popup x:Name="CalendarPopup" PlacementTarget="{Binding ElementName=BtnCalendar}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True" VerticalOffset="10">
                        <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp8" UniformCornerRadius="8" Background="{DynamicResource MaterialDesignPaper}">
                            <Calendar Margin="-2" BorderThickness="0" Background="Black" Foreground="{DynamicResource MaterialDesignBody}"/>
                        </materialDesign:Card>
                    </Popup>
                </StackPanel>

                <Button x:Name="BtnAddReminder" Click="BtnAddReminder_Click" HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" ToolTip="Yeni Hatırlatıcı Ekle">
                    <materialDesign:PackIcon Kind="Plus" Width="24" Height="24"/>
                </Button>
            </Grid>
        </materialDesign:ColorZone>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0,1,0,0" BorderBrush="#40000000" Margin="0,10,0,0">

            <TabItem Header="Aktif Görevler">
                <ListBox x:Name="LstActiveReminders" HorizontalContentAlignment="Stretch" SelectionChanged="LstReminders_SelectionChanged" Background="Transparent" BorderThickness="0" Margin="0,10,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="5,5,10,5" Padding="0" materialDesign:ElevationAssist.Elevation="Dp3">
                                <StackPanel>
                                    <Grid Margin="15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="18"/>
                                            <TextBlock Text="{Binding EventDate, StringFormat='dd.MM.yyyy HH:mm'}" Foreground="Gray" Margin="0,5,0,0"/>
                                        </StackPanel>
                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsActive}" Click="TglActive_Click" Style="{StaticResource MaterialDesignSwitchToggleButton}" ToolTip="Görevi Arşive Kaldır"/>
                                    </Grid>

                                    <Separator Style="{StaticResource MaterialDesignLightSeparator}"/>
                                    <Border Background="#F5F5F5" Padding="10,5">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource MaterialDesignFlatButton}" Foreground="LimeGreen" Content="✔ TAMAMLANDI" Click="BtnComplete_Click"/>
                                            <Button Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red" Content="X SİL" Click="BtnQuickDelete_Click"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="Arşiv">
                <ListBox x:Name="LstArchiveReminders" HorizontalContentAlignment="Stretch" SelectionChanged="LstReminders_SelectionChanged" Background="Transparent" BorderThickness="0" Margin="0,10,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="5,5,10,5" Padding="0" materialDesign:ElevationAssist.Elevation="Dp1" Opacity="0.8">
                                <StackPanel>
                                    <Grid Margin="15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" Foreground="DarkOrange" FontWeight="SemiBold" FontSize="16"/>
                                            <TextBlock Text="Durduruldu / Beklemede" Foreground="Gray" FontSize="11" Margin="0,5,0,0"/>
                                        </StackPanel>

                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsActive}" Click="TglActive_Click" Style="{StaticResource MaterialDesignSwitchToggleButton}" ToolTip="Tekrar Aktif Et"/>
                                    </Grid>
                                    <Separator Style="{StaticResource MaterialDesignLightSeparator}"/>
                                    <Border Background="#F5F5F5" Padding="10,5">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red" Content="KALICI OLARAK SİL" Click="BtnQuickDelete_Click"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="Tamamlananlar">
                <ListBox x:Name="LstCompletedReminders" HorizontalContentAlignment="Stretch" SelectionChanged="LstReminders_SelectionChanged" Background="Transparent" BorderThickness="0" Margin="0,10,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="5,5,10,5" Padding="15" materialDesign:ElevationAssist.Elevation="Dp1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <materialDesign:PackIcon Grid.Column="0" Kind="CheckCircle" Foreground="LimeGreen" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,15,0"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="Başarıyla Tamamlandı" Foreground="LimeGreen" FontSize="11" Margin="0,3,0,0"/>
                                    </StackPanel>

                                    <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconButton}" ToolTip="Geçmişten Sil" Click="BtnQuickDelete_Click">
                                        <materialDesign:PackIcon Kind="Delete" Foreground="Gray"/>
                                    </Button>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>

        </TabControl>
    </Grid>
</Window>